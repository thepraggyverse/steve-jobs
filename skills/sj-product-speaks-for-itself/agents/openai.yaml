interface:
  display_name: "Product Speaks For Itself"
  short_description: "Remove design that requires explanation to make sense"
  default_prompt: "Use $sj-product-speaks-for-itself to audit this onboarding flow."

policy:
  allow_implicit_invocation: false
