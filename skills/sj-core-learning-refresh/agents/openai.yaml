interface:
  display_name: "Core Learning Refresh"
  short_description: "Audit saved SJ lessons for drift and duplication"
  default_prompt: "Use $sj-core-learning-refresh to audit saved SJ learnings before we reuse them in this product strategy."

policy:
  allow_implicit_invocation: false
