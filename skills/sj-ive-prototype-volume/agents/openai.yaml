interface:
  display_name: "Ive Prototype Volume"
  short_description: "Replace debate with many concrete prototypes"
  default_prompt: "Use $sj-ive-prototype-volume to turn this design debate into a prototype selection loop."

policy:
  allow_implicit_invocation: false
