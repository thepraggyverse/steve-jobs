interface:
  display_name: "Core Catalog"
  short_description: "Choose the right Steve Jobs operating skill and sequence"
  default_prompt: "Use $sj-core-catalog to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
