interface:
  display_name: "Story Three Act Launch"
  short_description: "Build the problem, meaning, solution arc"
  default_prompt: "Use $sj-story-three-act-launch to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
