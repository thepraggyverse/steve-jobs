interface:
  display_name: "Strategy Failure Apprenticeship"
  short_description: "Turn failure into operating wisdom without flattering it"
  default_prompt: "Use $sj-strategy-failure-apprenticeship to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
