interface:
  display_name: "Ive Better Not Different"
  short_description: "Test whether novelty makes the product genuinely better"
  default_prompt: "Use $sj-ive-better-not-different to test whether this unusual design choice is actually better."

policy:
  allow_implicit_invocation: false
