interface:
  display_name: "Product Simplify To One"
  short_description: "Reduce a product, path, message, or priority set to one clear..."
  default_prompt: "Use $sj-product-simplify-to-one to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
