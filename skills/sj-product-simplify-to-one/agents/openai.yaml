interface:
  display_name: "Product Simplify To One"
  short_description: "Reduce competing priorities to one clear product path"
  default_prompt: "Use $sj-product-simplify-to-one to find the one thing this MVP should do."

policy:
  allow_implicit_invocation: false
