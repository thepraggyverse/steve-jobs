interface:
  display_name: "Product Customer Backwards"
  short_description: "Start from the user experience and work back to technology"
  default_prompt: "Use $sj-product-customer-backwards to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
