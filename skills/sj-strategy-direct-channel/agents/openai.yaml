interface:
  display_name: "Strategy Direct Channel"
  short_description: "Build a direct relationship with customers"
  default_prompt: "Use $sj-strategy-direct-channel to design our direct customer loop."

policy:
  allow_implicit_invocation: false
