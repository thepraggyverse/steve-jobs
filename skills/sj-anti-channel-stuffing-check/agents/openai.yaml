interface:
  display_name: "Anti Channel Stuffing Check"
  short_description: "Separate real traction from fake comfort"
  default_prompt: "Use $sj-anti-channel-stuffing-check to audit these growth numbers."

policy:
  allow_implicit_invocation: false
