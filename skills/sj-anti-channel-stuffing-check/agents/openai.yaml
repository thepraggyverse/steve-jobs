interface:
  display_name: "Anti Channel Stuffing Check"
  short_description: "Separate real traction from fake comfort"
  default_prompt: "Use $sj-anti-channel-stuffing-check to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
