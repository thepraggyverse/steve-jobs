interface:
  display_name: "Core Compound Learning"
  short_description: "Capture approved reusable lessons after each SJ skill run"
  default_prompt: "Use $sj-core-compound-learning to extract reusable Jobs lessons from this session."

policy:
  allow_implicit_invocation: false
