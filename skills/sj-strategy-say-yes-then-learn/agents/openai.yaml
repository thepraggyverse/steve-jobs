interface:
  display_name: "Strategy Say Yes Then Learn"
  short_description: "Commit before full certainty when the risk is survivable"
  default_prompt: "Use $sj-strategy-say-yes-then-learn to decide if this is a stretch yes."

policy:
  allow_implicit_invocation: false
