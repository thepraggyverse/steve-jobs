interface:
  display_name: "Strategy Pivot To Core"
  short_description: "Find the real business after the first plan fails"
  default_prompt: "Use $sj-strategy-pivot-to-core to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
