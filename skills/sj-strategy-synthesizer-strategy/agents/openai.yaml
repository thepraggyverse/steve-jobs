interface:
  display_name: "Strategy Synthesizer Strategy"
  short_description: "Combine separate technologies into one coherent product"
  default_prompt: "Use $sj-strategy-synthesizer-strategy to apply this Steve Jobs operating pattern to my task."

policy:
  allow_implicit_invocation: true
